# 🧠 Java Algorithm & Functional Programming Roadmap

## 📌 목표
> 자료구조 기초를 완전히 다지고,  
> **람다(Lambda)** 와 **스트림(Stream)** 을 실전 수준으로 익히기 위한 로드맵입니다.  
> 모든 문제는 **Baekjoon Online Judge** 기준입니다.

---

## 🧩 Ⅰ. 자료구조 완전 정복 (기초 & 응용)

| 순서 | 문제 번호 | 문제명 | 주요 자료구조 | 핵심 포인트 | 난이도 |
|:----:|:-----------:|:----------------|:--------------|:-----------------------------|:---------:|
| ① | 11866 | 요세푸스 문제 0 | 큐 / 덱 | `poll` + `offer` 회전 로직 이해 | 🟢 실버4 |
| ② | 1021 | 회전하는 큐 | 덱 | `pollFirst`, `offerLast`로 양방향 회전 구현 | 🟢 실버3 |
| ③ | 2346 | 풍선 터뜨리기 | 덱 + 인덱스 관리 | 회전 응용 + 이동 거리 계산 | 🟡 실버3~2 |
| ④ | 9935 | 문자열 폭발 | 스택 | 문자 누적·폭발 감지, O(N) 스택 구현 | 🔴 골드4 |
| ⑤ | 7662 | 이중 우선순위 큐 | 힙(Heap) + 맵(Map) | min/max 힙 동기화 및 정합성 유지 | 🔴 골드4 |

> ✅ 큐·스택·힙·트리 등 기본 자료구조를 완벽히 체화하는 단계입니다.

---

## 🌊 Ⅱ. Stream 완전 정복

| 번호 | 문제명 | Stream 포인트 | 학습 포인트 |
|:----:|:----------|:--------------------------|:-------------------------------------------|
| #2910 | 빈도 정렬 | `groupingBy`, `counting`, `sorted` | Map → Stream 변환 및 빈도 정렬 패턴 숙달 |
| #19636 | 요요 시뮬레이션 | `map`, `reduce` | 누적 계산 로직 및 함수형 사고 확립 |
| #1181 | 단어 정렬 | `distinct`, `sorted`, `thenComparing` | 중복 제거 + 다중 정렬 체인 |
| #5635 | 생일 | `min`, `max`, `Comparator` | 객체 리스트의 최소/최대 비교 연습 |

> 💡 Stream의 핵심 연산(`map`, `filter`, `reduce`, `collect`, `min/max`)을 실전 예제 중심으로 학습합니다.

---

## ⚡ Ⅲ. Lambda 완전 정복

| 번호 | 문제명 | 난이도 | 람다 포인트 | 요약 |
|:----:|:-----------:|:------------:|:-----------------------------|:-------------------------------------------|
| #10814 | 나이순 정렬 | Silver V | `(a, b) -> a.age - b.age` | Comparator를 람다식으로 표현, 코드 간결화 |
| #1181 | 단어 정렬 | Silver V | `thenComparing()` 체인 | 길이 → 사전순 정렬을 람다식으로 구현 |
| #2751 | 수 정렬하기 2 | Silver V | `list.sort((a, b) -> a - b)` | Comparator 기본 람다식 문법 연습 |
| #5635 | 생일 | Silver V | `Comparator.comparing((p) -> p.year)` | 객체 정렬 시 `Comparator.comparing()` 직접 활용 |

> ⚙️ 람다 문법, 메서드 레퍼런스(`::`), `Comparator` 체인을 완벽히 익히는 구간입니다.

---

## 🔁 Ⅳ. Stream + Lambda 통합 실습

| 문제 | 연습 방식 | 학습 포인트 |
|:------|:--------------|:-------------------------------|
| #1181 단어 정렬 | 🧩 두 가지 버전으로 구현 | 람다 vs Stream 비교 (`sorted(Comparator.comparing(...))`) |
| #5635 생일 | 🧩 두 가지 버전으로 구현 | `Comparator.comparing()` + `stream().min()` 응용 |

> 🧠 동일 문제를 **람다 버전**과 **스트림 버전**으로 각각 구현해보며  
> 함수형 사고와 선언적 코딩 스타일의 차이를 체득합니다.

---

## 📘 학습 순서 가이드

| 단계 | 범위 | 학습 포인트 |
|------|------|--------------|
| 1️⃣ | 자료구조 ①~⑤ | 구현력 및 기본 로직 리프레시 |
| 2️⃣ | Lambda (2751 → 10814 → 1181 람다 버전) | 람다식 문법 및 Comparator 체인 익히기 |
| 3️⃣ | Stream (1181 → 2910 → 5635 → 19636) | Stream 변환, 집계, 정렬, reduce 체계 정립 |
| 4️⃣ | Stream + Lambda 통합 (#1181, #5635) | 두 접근법의 코드 스타일 비교 및 장단점 분석 |
| 5️⃣ | 복습 및 기록화 | Notion / GitHub에 문제별 풀이 및 포인트 정리 |

---

## 🧾 핵심 학습 맵

| 문법 요소 | 커버 문제 |
|------------|------------|
| `Comparator.comparing`, `thenComparing` | #10814, #1181, #5635 |
| `map`, `filter`, `reduce` | #19636 |
| `groupingBy`, `counting`, `sorted` | #2910 |
| `distinct` | #1181 |
| `min`, `max` | #5635 |

---

### ✨ 결과 목표
- **자료구조**: 실버~골드 문제 난이도 완전 커버  
- **람다**: Comparator 및 정렬 체인 문법 자유자재  
- **스트림**: map/filter/reduce/collect 등 실무 레벨로 활용 가능  
- **통합 응용력**: 자바 함수형 프로그래밍 감각 완성
